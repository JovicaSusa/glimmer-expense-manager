<div>
  <table>
    <thead>
      <th>
        <td>Date</td>
        <td>Title</td>
        <td>Amount</td>
      </th>
    </thead>
    <tbody>
      {{#each @expenses key="@index" as |expense|}}
        <tr>
          <td>{{expense.date}}</td>
          <td>{{expense.title}}</td>
          <td>{{expense.amount}}</td>
        </tr>
      {{/each}}
    </tbody>
    <tfooter>
    </tfooter>
  </table>
</div>
